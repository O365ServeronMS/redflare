node_modules/
dist/
.wrangler/
.env
.dev.vars
.DS_Store

# Generated by `wrangler types` (npm run cf-typegen) from wrangler.toml --
# regenerate, don't hand-edit or commit. `npm run worker:typecheck` runs
# cf-typegen itself, so a fresh clone doesn't need this checked in.
worker-configuration.d.ts

# AI assistant context / memory — kept local, not in the repo
# (CLAUDE.md is tracked — it carries project guidance)
CLAUDE.local.md
agents.md
AGENTS.md
.claude/
**/memory/
state.md
